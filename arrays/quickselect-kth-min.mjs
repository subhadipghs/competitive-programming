"use strict"

import assert from 'assert';

/**
* Returns the kth minimum element from the unsorted array
* It uses quick select algorithm which is also used in quick sort algorithm
*
* @param {number[]} arr - is the list of elements from where element to be selected
* @param {number}   k -   n-th minimum
* @returns {number} the kth minimum element from arr
*/
const kthMinQuickSelect = (arr, k) => {

  
  return null;
}


assert.equal(kthMinQuickSelect([7, 12, 8, 4, 21, -4, 0, 48], 3), 4, 'should return 4 as it is the 3rd minimum element')
